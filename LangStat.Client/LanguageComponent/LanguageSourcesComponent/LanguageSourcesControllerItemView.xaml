<UserControl x:Class="LangStat.Client.LanguageSourcesComponent.LanguageSourcesControllerItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="50" />
        </Grid.ColumnDefinitions> 
        <Label Grid.Column="0" Content="{Binding Address, UpdateSourceTrigger=PropertyChanged}" />
        <Label Grid.Column="1" Content="{Binding LastUpdate, UpdateSourceTrigger=PropertyChanged}" />
        <ProgressBar Grid.Column="2" />
    </Grid>
</UserControl>
