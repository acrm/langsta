<UserControl x:Class="LangStat.Client.LanguageComponent.LanguageStatisticsComponent.LanguageStatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Margin="3">Статистика</TextBlock>
            <Button Margin="3" Command="{Binding UpdateCommand}">Обновить</Button>
        </StackPanel>
        <Border Grid.Row="1" BorderBrush="Silver" BorderThickness="1">
            <ScrollViewer>
                <TextBox Padding="5" Text="{Binding Output, UpdateSourceTrigger=PropertyChanged}"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
